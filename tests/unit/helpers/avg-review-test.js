import { avgReview } from 'dronez-ember/helpers/avg-review';
import { module, test } from 'qunit';

module('Unit | Helper | avg review');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = avgReview([42]);
  assert.ok(result);
});
